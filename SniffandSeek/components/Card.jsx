import { View, StyleSheet } from "react-native";

export default function Card() {
  return <View style={styles.container}>123</View>;
}

const styles = StyleSheet.create({
  container: {
    width: 200,
    height: 300,
    backgroundColor: "#333",
  },
});
